# E4 Whitelist Discord Bot

Gateway bot that posts the form embed and runs sequential Q&A in private threads.

See [docs/discord-whitelist.md](../docs/discord-whitelist.md).

```bash
cp .env.example .env
# fill DISCORD_BOT_TOKEN, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY
npm install
npm start
```
